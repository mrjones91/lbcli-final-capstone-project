# what is the coinbase tx in this block 243,834
bitcoin-cli -signet decoderawtransaction $(bitcoin-cli -signet getrawtransaction $(bitcoin-cli -signet getblock $(bitcoin-cli -signet getblockhash 243834) | jq -r '.tx[0]' )) | jq -r '.txid'